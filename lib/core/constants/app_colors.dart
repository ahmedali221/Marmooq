import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const Color brand = Color(0xFF007A3D);

  // Derived tints/shades
  static const Color brandDark = Color(0xFF005F2F);
  static const Color brandLight = Color(0xFFDBF1E6);
  static const Color brandMuted = Color(0xFFB7DEC9);
}
